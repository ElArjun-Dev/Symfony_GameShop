{{ form_start(form) }}

<div class="d-flex flex-column justify-content-center">
	<div>
		{# Appelle du formulaire grace a la fonction form_widget #}
		{{ form_widget(form) }}
	</div>
	<div>
		<button class="form-control btn btn-success mb-5">
			{{ button_label|default('Enregistrer')}}</button>
	</div>
</div>
{# fin du formulaire #}
{{ form_end(form) }}