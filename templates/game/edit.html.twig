{# on recupere notre base #}
{% extends "base.html.twig" %}

{% block title %}
	Edition de jeu
{% endblock %}

{% block body %}
	<h1>Modifier le jeu</h1>
	<h2>{{ game.title }}</h2>

	{{ include('game/_form.html.twig', {'button_label':'Modifier'}) }}

	<div class="d-flex">
		<a href="{{path('app_admin') }}">
			Retour a la liste</a>
		{% include "game/_delete_form.html.twig" %}
	</div>
{% endblock %}
